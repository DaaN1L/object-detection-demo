# Object detection with YOLOv8 demo

## Installation
All the necessary dependencies are in *poetry.lock*
```
pip install poetry
poetry install
```

## Usage

Activate the virtual environment
```
poetry shell
```

To start the server locally:
```
streamlit run app.py
```
